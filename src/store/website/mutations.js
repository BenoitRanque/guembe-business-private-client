export function PAGE (state, page) {
  state.page = page
}

export function LOCALE (state, locale) {
  state.locale = locale
}

export function LOCALES (state, locales) {
  state.locales = locales
}

export function FORMATS (state, formats) {
  state.formats = formats
}
